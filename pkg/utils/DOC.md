# pkg/utils package map

## Domain Summary
- Public wrapper exposing selected `internal/utils` functionality (currently logging) for external SDK consumers.

## Files (alphabetical)

### api.go
- Re-exports logger types and factories (`Logger`, `LogLevel`, `NewLogger`, `SetDefaultLogger`, `GetDefaultLogger`) for use outside the internal packages.

## Subdirectories
- _None._
